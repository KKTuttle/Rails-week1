<%= render "layouts/errors", :object => @definition %>

<%=form_for [@user, @word, @definition], remote: true, id: "add_new_definition" do |f| %>
  <%= f.label :content, "Meaning/Definition" %>
  <%= f.text_field :content %>
  <%= f.submit "Save"%>
<%end%>
